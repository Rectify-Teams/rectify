export function About() {
  return (
    <div>
      <h1>📖 About</h1>
      <p>This example demonstrates <strong>@rectify-dev/router</strong>.</p>
      <ul>
        <li><code>BrowserRouter</code> — HTML5 history API</li>
        <li><code>Routes</code> / <code>Route</code> — declarative matching</li>
        <li><code>Link</code> / <code>NavLink</code> — client-side navigation</li>
        <li><code>Outlet</code> — nested layout routes</li>
        <li><code>useParams</code> — URL parameters</li>
        <li><code>Navigate</code> — imperative redirect</li>
      </ul>
    </div>
  );
}
